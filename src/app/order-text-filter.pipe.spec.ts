import { OrderTextFilterPipe } from './order-text-filter.pipe';

describe('OrderTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
